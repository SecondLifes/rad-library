# docs/images/

Generated README banner PNGs land here — `overview.png`, `core-features.png`,
`design-philosophy.png` — see `Prompts/image-prompts.md` /
`Prompts/image-prompts.tr-TR.md` for the prompts that produce them. Resize oversized model output before
committing (no bundled script for this in a kit -- that tooling stays in
the AI-Spec-Kits-Maker workspace itself).

This file exists only so the empty folder survives being copied as part
of the blank scaffold (Git and plain folder copies don't preserve
directories with nothing in them) — delete it once real images are
added, or leave it, it's harmless either way.
